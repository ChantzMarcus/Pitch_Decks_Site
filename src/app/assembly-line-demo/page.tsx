import CinematicAssemblyLine from '@/components/CinematicAssemblyLine';

export default function AssemblyLineDemoPage() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-ivory to-old-gold">
      <CinematicAssemblyLine />
    </div>
  );
}